
software {
	var a = number(read(' '))
	var b = number(read('\n'))
	
	if a = b
		print( a, " is equal to ", b)
	elseif a < b
		print( a, " is smaller than ", b)
	elseif a > b
		print( a, " is greater than ", b)
	end
}
