
software {
	var a = number(reada(' '))
	var b = number(reada('\n'))
	
	if a = b
		print( a, " is equal to ", b)
	elseif a < b
		print( a, " is smaller than ", b)
	elseif a > b
		print( a, " is greater than ", b)
	end
}
